package com.proyecto1.common.enums;

public enum MessageType {
    TEXT,      // Mensaje de texto
    IMAGE,     // Imagen
    FILE,      // Archivo adjunto
    SYSTEM     // Mensaje generado automáticamente (ej. "Usuario se unió")
}
